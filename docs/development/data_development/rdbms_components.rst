.. _rdbms_components:

RDBMS Components
================

RDBMS Drop
----------

:class:`RDBMSDrop <dlg.drop.RDBMSDrop>` is a generic relational database storage drop that uses SQL
to insert and select data.